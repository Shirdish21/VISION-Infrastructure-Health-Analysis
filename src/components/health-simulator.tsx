
"use client";

import { useEffect } from "react";

/**
 * HealthSimulator is now deactivated to prevent flickering.
 * All health scoring is now handled during asset creation or manual admin updates.
 */
export default function HealthSimulator() {
  useEffect(() => {
    // Continuous simulation logic removed per stability requirements.
    console.log("VISION Intelligence: Continuous simulation suspended. Using stable data model.");
  }, []);

  return null;
}
